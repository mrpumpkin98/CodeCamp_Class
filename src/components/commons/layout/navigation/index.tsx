import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: green;
`;

export default function LayoutNavigation(): JSX.Element {
  return <Wrapper>여기는 내비게이션이지라</Wrapper>;
}
